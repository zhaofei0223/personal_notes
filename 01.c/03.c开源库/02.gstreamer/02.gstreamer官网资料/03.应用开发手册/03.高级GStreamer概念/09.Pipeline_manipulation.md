# 管道操控

本章将介绍从应用程序中操作管道的多种方法。以下是我们将要涵盖的部分主题：

- 如何将应用程序中的数据插入到管道中
- 如何从管道中读取数据
- 如何控制管道的速度、长度和起点
- 如何监听管道的数据处理过程。

本章部分内容比较底层，因此您需要一些编程经验和对 GStreamer 的良好理解才能理解。

## 使用探针

探测功能可以理解为对一个触控板监听器的访问。从技术上讲，探测器只是一个回调函数，可以通过 `add` 函数附加到触控板上 gst_pad_add_probe ()。反之，也可以使用gst_pad_remove_probe ()`remove` 函数移除回调函数。附加后，探测器会在触控板上发生任何活动时通知您。添加探测器时，您可以定义您感兴趣的通知类型。

探针类型：

- 缓冲区会被推送或拉取。注册探针时需要指定推送或拉取操作， GST_PAD_PROBE_TYPE_BUFFER因为焊盘可以采用不同的调度方式。您还可以使用可选的 `--schedule-mode` GST_PAD_PROBE_TYPE_PUSH和 ` GST_PAD_PROBE_TYPE_PULL--distribution-mode` 标志来指定您感兴趣的调度模式。您可以使用此探针来检查、修改或删除缓冲区。请参阅 “数据探针”。

- 缓冲区列表已被推送。GST_PAD_PROBE_TYPE_BUFFER_LIST 注册探针时请使用该列表。

- 事件会沿着缓冲区传递。使用 ` GST_PAD_PROBE_TYPE_EVENT_DOWNSTREAMand` GST_PAD_PROBE_TYPE_EVENT_UPSTREAM标志可以选择下游事件和上游事件。此外，还可以方便地 GST_PAD_PROBE_TYPE_EVENT_BOTH接收双向事件的通知。默认情况下，刷新事件不会触发通知。您需要显式启用 GST_PAD_PROBE_TYPE_EVENT_FLUSH才能接收来自刷新事件的回调。事件始终仅在推送模式下触发通知。您可以使用这种类型的探测来检查、修改或丢弃事件。

- 查询会遍历一个数据位。使用 ` GST_PAD_PROBE_TYPE_QUERY_DOWNSTREAMand` GST_PAD_PROBE_TYPE_QUERY_UPSTREAM标志可以选择下游查询和上游查询。 GST_PAD_PROBE_TYPE_QUERY_BOTH也可以使用 `for` 选项同时选择两个方向。查询探测会收到两次通知：一次是查询向上游/下游移动时，另一次是查询结果返回时。您可以使用 `and` 标志分别选择在GST_PAD_PROBE_TYPE_PUSH查询 GST_PAD_PROBE_TYPE_PULL执行时和查询结果返回时调用回调函数。

- 您可以使用查询探测来检查或修改查询，甚至可以在探测回调函数中回答查询。要回答查询，您需要将结果值放入查询中，然后GST_PAD_PROBE_DROP从回调函数中返回。

- 除了通知您数据流之外，您还可以让探针在回调返回时阻塞数据流。这称为阻塞探针，可通过指定 GST_PAD_PROBE_TYPE_BLOCK标志激活。您可以将此标志与其他标志一起使用，以便仅阻塞选定活动的数据流。如果您移除探针或从 GST_PAD_PROBE_REMOVE回调返回，则数据流将再次解除阻塞。您可以通过从回调返回来仅允许当前阻塞的项目通过GST_PAD_PROBE_PASS，它将在处理下一个项目时再次阻塞。

- 阻塞探针用于临时阻塞焊盘，因为这些焊盘已断开连接或您即将断开它们的连接。如果数据流未被阻塞，则当数据被推送到未连接的焊盘时，管道将进入错误状态。我们将了解如何使用阻塞探针来部分预滚动管道。另请参阅“播放媒体文件的一部分”。

- 当焊盘上没有任何活动时，您将收到通知。您可以使用标志安装此探针GST_PAD_PROBE_TYPE_IDLE。您可以指定 GST_PAD_PROBE_TYPE_PUSH仅GST_PAD_PROBE_TYPE_PULL接收特定焊盘调度模式的通知。IDLE 探针也是一个阻塞探针，也就是说，只要 IDLE 探针处于安装状态，它就会阻止任何数据通过该焊盘。

您可以使用空闲探针动态地重新连接焊盘。我们将了解如何使用空闲探针替换流水线中的元素。另请参阅 “动态更改流水线”。

### 数据探测

数据探测会在数据通过缓冲区时通知您。 要创建此类探测，请传递GST_PAD_PROBE_TYPE_BUFFER`and/to` 参数GST_PAD_PROBE_TYPE_BUFFER_LIST。 大多数常见的缓冲区操作元素可以在函数中执行，也可以在探测回调函数中执行。gst_pad_add_probe ()_chain ()

数据探测在管道的流式线程上下文中运行，因此回调函数应尽量避免阻塞，并且通常应避免执行异常操作。这样做可能会对管道的性能产生负面影响，或者在出现错误的情况下导致死锁或崩溃。更准确地说，通常应避免在探测回调函数中调用与 GUI 相关的函数，也不要尝试更改管道的状态。应用程序可以通过管道总线发布自定义消息来与主应用程序线程通信，并使其执行诸如停止管道之类的操作。

以下是使用数据探测的示例。gst-launch-1.0 videotestsrc ! xvimagesink如果您不确定要查找什么，请将此程序的输出与以下输出进行比较：

```c
#include <gst/gst.h>

static GstPadProbeReturn
cb_have_data (GstPad          *pad,
              GstPadProbeInfo *info,
              gpointer         user_data)
{
  gint x, y;
  GstMapInfo map;
  guint16 *ptr, t;
  GstBuffer *buffer;

  buffer = GST_PAD_PROBE_INFO_BUFFER (info);

  buffer = gst_buffer_make_writable (buffer);

  /* Making a buffer writable can fail (for example if it
   * cannot be copied and is used more than once)
   */
  if (buffer == NULL)
    return GST_PAD_PROBE_OK;

  /* Mapping a buffer can fail (non-writable) */
  if (gst_buffer_map (buffer, &map, GST_MAP_WRITE)) {
    ptr = (guint16 *) map.data;
    /* invert data */
    for (y = 0; y < 288; y++) {
      for (x = 0; x < 384 / 2; x++) {
        t = ptr[384 - 1 - x];
        ptr[384 - 1 - x] = ptr[x];
        ptr[x] = t;
      }
      ptr += 384;
    }
    gst_buffer_unmap (buffer, &map);
  }

  GST_PAD_PROBE_INFO_DATA (info) = buffer;

  return GST_PAD_PROBE_OK;
}

gint
main (gint   argc,
      gchar *argv[])
{
  GMainLoop *loop;
  GstElement *pipeline, *src, *sink, *filter, *csp;
  GstCaps *filtercaps;
  GstPad *pad;

  /* init GStreamer */
  gst_init (&argc, &argv);
  loop = g_main_loop_new (NULL, FALSE);

  /* build */
  pipeline = gst_pipeline_new ("my-pipeline");
  src = gst_element_factory_make ("videotestsrc", "src");
  if (src == NULL)
    g_error ("Could not create 'videotestsrc' element");

  filter = gst_element_factory_make ("capsfilter", "filter");
  g_assert (filter != NULL); /* should always exist */

  csp = gst_element_factory_make ("videoconvert", "csp");
  if (csp == NULL)
    g_error ("Could not create 'videoconvert' element");

  sink = gst_element_factory_make ("xvimagesink", "sink");
  if (sink == NULL) {
    sink = gst_element_factory_make ("ximagesink", "sink");
    if (sink == NULL)
      g_error ("Could not create neither 'xvimagesink' nor 'ximagesink' element");
  }

  gst_bin_add_many (GST_BIN (pipeline), src, filter, csp, sink, NULL);
  gst_element_link_many (src, filter, csp, sink, NULL);
  filtercaps = gst_caps_new_simple ("video/x-raw",
               "format", G_TYPE_STRING, "RGB16",
               "width", G_TYPE_INT, 384,
               "height", G_TYPE_INT, 288,
               "framerate", GST_TYPE_FRACTION, 25, 1,
               NULL);
  g_object_set (G_OBJECT (filter), "caps", filtercaps, NULL);
  gst_caps_unref (filtercaps);

  pad = gst_element_get_static_pad (src, "src");
  gst_pad_add_probe (pad, GST_PAD_PROBE_TYPE_BUFFER,
      (GstPadProbeCallback) cb_have_data, NULL, NULL);
  gst_object_unref (pad);

  /* run */
  gst_element_set_state (pipeline, GST_STATE_PLAYING);

  /* wait until it's up and running or failed */
  if (gst_element_get_state (pipeline, NULL, NULL, -1) == GST_STATE_CHANGE_FAILURE) {
    g_error ("Failed to go into PLAYING state");
  }

  g_print ("Running ...\n");
  g_main_loop_run (loop);

  /* exit */
  gst_element_set_state (pipeline, GST_STATE_NULL);
  gst_object_unref (pipeline);

  return 0;
}
```

严格来说，只有当缓冲区可写时，填充探测回调函数才能修改缓冲区内容。缓冲区是否可写很大程度上取决于流水线及其涉及的元素。通常情况下，缓冲区是可写的，但有时并非如此。如果缓冲区不可写，则对数据或元数据的意外修改可能会引入难以调试和追踪的错误。您可以使用 `.` 检查缓冲区是否可写gst_buffer_is_writable ()。由于您可以返回与传入缓冲区不同的缓冲区，因此最好在回调函数中使用 `.` 将缓冲区设置为可写gst_buffer_make_writable ()。

Pad 探针最适合用于查看数据在管道中传输的过程。如果需要修改数据，则应该编写自己的 GStreamer 元素。像 `GStreamer.PadProbe` 或 `GStreamer.PadProbe` 这样的基类GstAudioFilter可以GstVideoFilter简化 GstBaseTransform这一过程。

如果你只想检查缓冲区在流水线中的流转情况，甚至不需要设置焊盘探针。你也可以直接在流水线中插入一个标识元素，并连接到它的“交接”信号。标识元素还提供了一些有用的调试工具，例如 ` dumpand`last-message属性；后者可以通过向 `and` 传递 `-v` 开关gst-launch并将silent标识元素的 `and` 属性设置为 `true` 来启用FALSE。

### 播放媒体文件的某一部分

在这个例子中，我们将向您展示如何播放媒体文件的一部分。目标是只播放2到5秒的部分，然后退出。

首先，我们将一个uridecodebin元素添加到PAUSED状态中，并确保屏蔽所有已创建的源垫。当所有源垫都被屏蔽后，我们就拥有了所有源垫的数据，并声明该元素uridecodebin已预滚动。

在预录视频流中，我们可以获取媒体时长，也可以执行查找操作。我们希望对视频流执行查找操作，以选择 2 到 5 秒的片段。

配置好所需的部分后，我们可以连接接收器元素，解除源焊盘的阻塞，并将管道设置为相应状态PLAYING。您会看到，在流向目标之前，接收器会显示请求的区域EOS。

以下是代码：

```c
#include <gst/gst.h>

static GMainLoop *loop;
static gint counter;
static GstBus *bus;
static gboolean prerolled = FALSE;
static GstPad *sinkpad;

static void
dec_counter (GstElement * pipeline)
{
  if (prerolled)
    return;

  if (g_atomic_int_dec_and_test (&counter)) {
    /* all probes blocked and no-more-pads signaled, post
     * message on the bus. */
    prerolled = TRUE;

    gst_bus_post (bus, gst_message_new_application (
          GST_OBJECT_CAST (pipeline),
          gst_structure_new_empty ("ExPrerolled")));
  }
}

/* called when a source pad of uridecodebin is blocked */
static GstPadProbeReturn
cb_blocked (GstPad          *pad,
            GstPadProbeInfo *info,
            gpointer         user_data)
{
  GstElement *pipeline = GST_ELEMENT (user_data);

  if (prerolled)
    return GST_PAD_PROBE_REMOVE;

  dec_counter (pipeline);

  return GST_PAD_PROBE_OK;
}

/* called when uridecodebin has a new pad */
static void
cb_pad_added (GstElement *element,
              GstPad     *pad,
              gpointer    user_data)
{
  GstElement *pipeline = GST_ELEMENT (user_data);

  if (prerolled)
    return;

  g_atomic_int_inc (&counter);

  gst_pad_add_probe (pad, GST_PAD_PROBE_TYPE_BLOCK_DOWNSTREAM,
      (GstPadProbeCallback) cb_blocked, pipeline, NULL);

  /* try to link to the video pad */
  gst_pad_link (pad, sinkpad);
}

/* called when uridecodebin has created all pads */
static void
cb_no_more_pads (GstElement *element,
                 gpointer    user_data)
{
  GstElement *pipeline = GST_ELEMENT (user_data);

  if (prerolled)
    return;

  dec_counter (pipeline);
}

/* called when a new message is posted on the bus */
static void
cb_message (GstBus     *bus,
            GstMessage *message,
            gpointer    user_data)
{
  GstElement *pipeline = GST_ELEMENT (user_data);

  switch (GST_MESSAGE_TYPE (message)) {
    case GST_MESSAGE_ERROR:
      g_print ("we received an error!\n");
      g_main_loop_quit (loop);
      break;
    case GST_MESSAGE_EOS:
      g_print ("we reached EOS\n");
      g_main_loop_quit (loop);
      break;
    case GST_MESSAGE_APPLICATION:
    {
      if (gst_message_has_name (message, "ExPrerolled")) {
        /* it's our message */
        g_print ("we are all prerolled, do seek\n");
        gst_element_seek (pipeline,
            1.0, GST_FORMAT_TIME,
            GST_SEEK_FLAG_FLUSH | GST_SEEK_FLAG_ACCURATE,
            GST_SEEK_TYPE_SET, 2 * GST_SECOND,
            GST_SEEK_TYPE_SET, 5 * GST_SECOND);

        gst_element_set_state (pipeline, GST_STATE_PLAYING);
      }
      break;
    }
    default:
      break;
  }
}

gint
main (gint   argc,
      gchar *argv[])
{
  GstElement *pipeline, *src, *csp, *vs, *sink;

  /* init GStreamer */
  gst_init (&argc, &argv);
  loop = g_main_loop_new (NULL, FALSE);

  if (argc < 2) {
    g_print ("usage: %s <uri>", argv[0]);
    return -1;
  }

  /* build */
  pipeline = gst_pipeline_new ("my-pipeline");

  bus = gst_pipeline_get_bus (GST_PIPELINE (pipeline));
  gst_bus_add_signal_watch (bus);
  g_signal_connect (bus, "message", (GCallback) cb_message,
      pipeline);

  src = gst_element_factory_make ("uridecodebin", "src");
  if (src == NULL)
    g_error ("Could not create 'uridecodebin' element");

  g_object_set (src, "uri", argv[1], NULL);

  csp = gst_element_factory_make ("videoconvert", "csp");
  if (csp == NULL)
    g_error ("Could not create 'videoconvert' element");

  vs = gst_element_factory_make ("videoscale", "vs");
  if (vs == NULL)
    g_error ("Could not create 'videoscale' element");

  sink = gst_element_factory_make ("autovideosink", "sink");
  if (sink == NULL)
    g_error ("Could not create 'autovideosink' element");

  gst_bin_add_many (GST_BIN (pipeline), src, csp, vs, sink, NULL);

  /* can't link src yet, it has no pads */
  gst_element_link_many (csp, vs, sink, NULL);

  sinkpad = gst_element_get_static_pad (csp, "sink");

  /* for each pad block that is installed, we will increment
   * the counter. for each pad block that is signaled, we
   * decrement the counter. When the counter is 0 we post
   * an app message to tell the app that all pads are
   * blocked. Start with 1 that is decremented when no-more-pads
   * is signaled to make sure that we only post the message
   * after no-more-pads */
  g_atomic_int_set (&counter, 1);

  g_signal_connect (src, "pad-added",
      (GCallback) cb_pad_added, pipeline);
  g_signal_connect (src, "no-more-pads",
      (GCallback) cb_no_more_pads, pipeline);

  gst_element_set_state (pipeline, GST_STATE_PAUSED);

  g_main_loop_run (loop);

  gst_element_set_state (pipeline, GST_STATE_NULL);

  gst_object_unref (sinkpad);
  gst_object_unref (bus);
  gst_object_unref (pipeline);
  g_main_loop_unref (loop);

  return 0;
}
```

请注意，我们使用自定义应用程序消息来通知主线程该片段 uridecodebin已预滚动。主线程随后会向请求区域发出刷新寻道请求。刷新操作会暂时解除对填充的阻塞，并在新数据再次到达时重新阻塞填充。我们检测到第二次阻塞后会移除探测数据。然后，我们将管道设置为PLAYING播放选定的 2 到 5 秒片段；应用程序等待EOS消息并退出。

## 手动向管道添加或删除数据

许多人希望使用自己的数据源向管道注入数据，也有人希望获取管道的输出并在自己的应用程序中进行处理。虽然强烈不建议这样做，但 GStreamer 仍然支持这些方法——务必小心！您必须清楚自己在做什么。由于没有基类的支持，您需要彻底理解状态变更和同步机制。如果操作不当，可能会造成各种问题。最好的办法始终是编写插件，并让基类来管理它。有关此主题的更多信息，请参阅插件编写指南。此外，请查看下一节，其中解释了如何在应用程序中静态嵌入插件。

您可以使用两种元素来实现上述目的：（appsrc虚拟源）和appsink（虚拟汇）。相同的方法适用于这些元素。我们将讨论如何使用它们从管道中插入（使用 `insert`appsrc或 `grave` ），以及如何设置协商。appsink

两者appsrc都appsink提供了两套 API。一套 API 使用标准 GObject（动作）信号和属性。同样的 API 也以常规 C API 的形式提供。C API 性能更高，但需要链接到应用程序库才能使用其中的元素。

### 使用 appsrc 插入数据

让我们来看看appsrc如何将应用程序数据插入到管道中。

appsrc它有一些配置选项，可以控制其运行方式。您应该决定以下内容：

将以appsrc推送或拉取模式运行。该stream-type 属性可用于控制此模式。Arandom-access stream-type 将appsrc激活拉取模式调度，而另一个值 将stream-types激活推送模式。

缓冲区appsrc输出容量的限制。这需要通过caps属性进行配置。此属性必须设置为固定容量，并将用于下游协商格式。

是否appsrc以实时模式运行。这通过属性进行配置is-live。以实时模式运行时，设置 `timestamp` 和 `timestamp` 属性也很重要min-latency。`timestamp`max-latency应 min-latency设置为从捕获缓冲区到将其推入 `map` 之间的时间appsrc。在实时模式下，您应该在将缓冲区传递给 `map` 之前，使用管道在捕获缓冲区第一个字节时为缓冲区添加时间戳。您可以使用`timestamp` 属性让 `map` 执行时间戳操作，但此时 `timestamp`必须设置为 0，因为`timestamp` 是基于 `map`捕获给定缓冲区时running-time 的状态生成的 。appsrcappsrcdo-timestampmin-latencyappsrcrunning-time

推送的 SEGMENT 事件的格式appsrc。此格式会影响缓冲区大小的running-time计算方式，因此您必须确保理解这一点。对于实时源，您可能需要将 format 属性设置为 `<timestamp>` GST_FORMAT_TIME。对于非实时源，则取决于您处理的媒体类型。如果您计划为缓冲区添加时间戳，则应使用 `<timestamp>`GST_FORMAT_TIME作为格式；否则，`<timestamp>`GST_FORMAT_BYTES可能更合适。

如果appsrc以随机访问模式运行，则必须将 size 属性配置为流中的字节数。这样，​​下游元素就能知道媒体的大小，并在需要时定位到流的末尾。

处理数据的主要方式appsrc是使用 gst_app_src_push_buffer ()函数或发出push-buffer操作信号。这会将缓冲区放入队列，appsrc流式线程将从中读取数据。需要注意的是，数据传输不会在执行push-buffer 调用的线程中进行。

该属性控制队列满 之前max-bytes可以放入多少数据。内部队列满时，系统会始终发出信号，告知应用程序停止向队列推送数据。该属性会导致方法阻塞，直到再次有可用数据为止。appsrcappsrcenough-dataappsrcblockappsrcpush-buffer

当内部队列数据不足时，need-data会发出信号，通知应用程序开始向其中推送更多数据appsrc。

除了 ` need-dataand`enough-data信号之外，当 ` property` 设置为 `or`时，appsrc还可以发出`signal` 信号。信号参数将包含流中新的期望位置，单位由 ` property` 属性设置。收到信号后，应用程序应从新位置推送缓冲区。seek-datastream-modeseekablerandom-accessformatseek-data

当最后一个字节被压​​入时appsrc，你必须调用 gst_app_src_end_of_stream ()它来向EOS下游发送一个字节。

这些信号使应用程序能够appsrc以推送和拉取模式运行，接下来将对此进行解释。

在推送模式下使用 appsrc
当appsrc配置为推送模式（stream-type流式或可寻址）时，应用程序会push-buffer使用新的缓冲区重复调用该方法。可选地，可以通过`stop` 和 `start` 信号分别停止/启动调用appsrc来控制enough-data队列大小。`queue` 属性的值 定义了发出信号之前内部队列需要清空多少。您可以将其设置为某个正值，以避免队列完全清空。need-datapush-buffermin-percentappsrcneed-data

seek-data当stream-type设置为 时，不要忘记实现回调函数GST_APP_STREAM_TYPE_SEEKABLE。

在实施各种网络协议或硬件设备时，请使用此模式。

### 在拉取模式下使用 appsrc

appsrc在拉取模式下，数据由信号处理程序提供need-data。您应该推送信号中请求的字节数 need-data。只有在数据流末尾时，才允许推送少于请求的字节数。

使用此模式可访问文件或其他随机可访问的资源。

### Appsrc 示例

本示例应用程序将生成黑白视频（每秒切换一次），并通过 Xv 窗口输出。我们使用appsrc带有强制格式限制的源文件。我们使用色彩空间转换元素来确保向 X 服务器提供正确的格式。我们配置了一个帧速率可变（0/1）的视频流，并设置了输出缓冲区的时间戳，以实现每秒播放 2 帧。

appsrc 注意，尽管appsrc系统运行在推送模式下，但我们如何使用拉取模式方法将新缓冲区推送到系统中。

```c
#include <gst/gst.h>

static GMainLoop *loop;

static void
cb_need_data (GstElement *appsrc,
          guint       unused_size,
          gpointer    user_data)
{
  static gboolean white = FALSE;
  static GstClockTime timestamp = 0;
  GstBuffer *buffer;
  guint size;
  GstFlowReturn ret;

  size = 385 * 288 * 2;

  buffer = gst_buffer_new_allocate (NULL, size, NULL);

  /* this makes the image black/white */
  gst_buffer_memset (buffer, 0, white ? 0xff : 0x0, size);

  white = !white;

  GST_BUFFER_PTS (buffer) = timestamp;
  GST_BUFFER_DURATION (buffer) = gst_util_uint64_scale_int (1, GST_SECOND, 2);

  timestamp += GST_BUFFER_DURATION (buffer);

  g_signal_emit_by_name (appsrc, "push-buffer", buffer, &ret);
  gst_buffer_unref (buffer);

  if (ret != GST_FLOW_OK) {
    /* something wrong, stop pushing */
    g_main_loop_quit (loop);
  }
}

gint
main (gint   argc,
      gchar *argv[])
{
  GstElement *pipeline, *appsrc, *conv, *videosink;

  /* init GStreamer */
  gst_init (&argc, &argv);
  loop = g_main_loop_new (NULL, FALSE);

  /* setup pipeline */
  pipeline = gst_pipeline_new ("pipeline");
  appsrc = gst_element_factory_make ("appsrc", "source");
  conv = gst_element_factory_make ("videoconvert", "conv");
  videosink = gst_element_factory_make ("xvimagesink", "videosink");

  /* setup */
  g_object_set (G_OBJECT (appsrc), "caps",
        gst_caps_new_simple ("video/x-raw",
                     "format", G_TYPE_STRING, "RGB16",
                     "width", G_TYPE_INT, 384,
                     "height", G_TYPE_INT, 288,
                     "framerate", GST_TYPE_FRACTION, 0, 1,
                     NULL), NULL);
  gst_bin_add_many (GST_BIN (pipeline), appsrc, conv, videosink, NULL);
  gst_element_link_many (appsrc, conv, videosink, NULL);

  /* setup appsrc */
  g_object_set (G_OBJECT (appsrc),
        "stream-type", 0,
        "format", GST_FORMAT_TIME, NULL);
  g_signal_connect (appsrc, "need-data", G_CALLBACK (cb_need_data), NULL);

  /* play */
  gst_element_set_state (pipeline, GST_STATE_PLAYING);
  g_main_loop_run (loop);

  /* clean up */
  gst_element_set_state (pipeline, GST_STATE_NULL);
  gst_object_unref (GST_OBJECT (pipeline));
  g_main_loop_unref (loop);

  return 0;
}
```

### 使用 appsink 获取数据

与 相比appsrc，appsink更容易使用一些。它还支持拉取和推送模式，可以从管道中获取数据。

从 appsink 获取样本的通常方法是使用 `getSample()` gst_app_sink_pull_sample()和 `getSample() gst_app_sink_pull_preroll()` 方法或使用 `getSample()`和pull-sample`getSample()`pull-preroll信号。这些方法会阻塞，直到 sink 中有样本可用，或者 sink 关闭或达到 `.` 为止EOS。

appsink内部会使用队列从流线程收集缓冲区。如果应用程序拉取样本的速度不够快，随着时间的推移，该队列会消耗大量内存。max-buffers 可以使用此属性来限制队列大小。该drop属性控制当达到最大队列大小时，流线程是阻塞还是丢弃较旧的缓冲区。请注意，阻塞流线程会对实时性能产生负面影响，因此应避免这样做。

如果不希望出现阻塞行为，将该emit-signals 属性设置为TRUE将使 appsink 在可以不阻塞地拉取样本时发出new-sample和 信号。new-preroll

该caps属性appsink可用于控制后者可以接收的格式。此属性可以包含非固定的上限值，提取的样本格式可以通过获取样本上限值来确定。

如果 pull-preroll 或 pull-sample 方法返回 true NULL，则程序 appsink已停止或处于暂停状态。您可以使用属性或方法来EOS检查程序状态。EOSeosgst_app_sink_is_eos()

该eos信号还可以用于在EOS达到目标状态时收到通知，从而避免轮询。

请考虑配置以下属性appsink：

sync如果您希望接收器基类在将样本传递给您之前将缓冲区与流水线时钟同步，则需要设置此属性。

启用服务质量 (QoS) 属性qos。如果您处理的是原始视频帧，并且让基类进行时钟同步，那么让基类向 QOS上游发送事件也是一个好主意。

caps 属性包含可接受的容量。上游元素会尝试转换格式，使其与配置的容量匹配appsink。您仍然必须检查GstSample才能获取缓冲区的实际容量。

### Appsink 示例

下面是一个使用以下方法捕获视频流快照的示例appsink。

```c
#include <gst/gst.h>
#ifdef HAVE_GTK
#include <gtk/gtk.h>
#endif

#include <stdlib.h>

#define CAPS "video/x-raw,format=RGB,width=160,pixel-aspect-ratio=1/1"

int
main (int argc, char *argv[])
{
  GstElement *pipeline, *sink;
  gint width, height;
  GstSample *sample;
  gchar *descr;
  GError *error = NULL;
  gint64 duration, position;
  GstStateChangeReturn ret;
  gboolean res;
  GstMapInfo map;

  gst_init (&argc, &argv);

  if (argc != 2) {
    g_print ("usage: %s <uri>\n Writes snapshot.png in the current directory\n",
        argv[0]);
    exit (-1);
  }

  /* create a new pipeline */
  descr =
      g_strdup_printf ("uridecodebin uri=%s ! videoconvert ! videoscale ! "
      " appsink name=sink caps=\"" CAPS "\"", argv[1]);
  pipeline = gst_parse_launch (descr, &error);

  if (error != NULL) {
    g_print ("could not construct pipeline: %s\n", error->message);
    g_clear_error (&error);
    exit (-1);
  }

  /* get sink */
  sink = gst_bin_get_by_name (GST_BIN (pipeline), "sink");

  /* set to PAUSED to make the first frame arrive in the sink */
  ret = gst_element_set_state (pipeline, GST_STATE_PAUSED);
  switch (ret) {
    case GST_STATE_CHANGE_FAILURE:
      g_print ("failed to play the file\n");
      exit (-1);
    case GST_STATE_CHANGE_NO_PREROLL:
      /* for live sources, we need to set the pipeline to PLAYING before we can
       * receive a buffer. We don't do that yet */
      g_print ("live sources not supported yet\n");
      exit (-1);
    default:
      break;
  }
  /* This can block for up to 5 seconds. If your machine is really overloaded,
   * it might time out before the pipeline prerolled and we generate an error. A
   * better way is to run a mainloop and catch errors there. */
  ret = gst_element_get_state (pipeline, NULL, NULL, 5 * GST_SECOND);
  if (ret == GST_STATE_CHANGE_FAILURE) {
    g_print ("failed to play the file\n");
    exit (-1);
  }

  /* get the duration */
  gst_element_query_duration (pipeline, GST_FORMAT_TIME, &duration);

  if (duration != -1)
    /* we have a duration, seek to 5% */
    position = duration * 5 / 100;
  else
    /* no duration, seek to 1 second, this could EOS */
    position = 1 * GST_SECOND;

  /* seek to the position in the file. Most files have a black first frame so
   * by seeking to somewhere else we have a bigger chance of getting something
   * more interesting. An optimisation would be to detect black images and then
   * seek a little more */
  gst_element_seek_simple (pipeline, GST_FORMAT_TIME,
      GST_SEEK_FLAG_KEY_UNIT | GST_SEEK_FLAG_FLUSH, position);

  /* get the preroll buffer from appsink, this block untils appsink really
   * prerolls */
  g_signal_emit_by_name (sink, "pull-preroll", &sample, NULL);

  /* if we have a buffer now, convert it to a pixbuf. It's possible that we
   * don't have a buffer because we went EOS right away or had an error. */
  if (sample) {
    GstBuffer *buffer;
    GstCaps *caps;
    GstStructure *s;

    /* get the snapshot buffer format now. We set the caps on the appsink so
     * that it can only be an rgb buffer. The only thing we have not specified
     * on the caps is the height, which is dependant on the pixel-aspect-ratio
     * of the source material */
    caps = gst_sample_get_caps (sample);
    if (!caps) {
      g_print ("could not get snapshot format\n");
      exit (-1);
    }
    s = gst_caps_get_structure (caps, 0);

    /* we need to get the final caps on the buffer to get the size */
    res = gst_structure_get_int (s, "width", &width);
    res |= gst_structure_get_int (s, "height", &height);
    if (!res) {
      g_print ("could not get snapshot dimension\n");
      exit (-1);
    }

    /* create pixmap from buffer and save, gstreamer video buffers have a stride
     * that is rounded up to the nearest multiple of 4 */
    buffer = gst_sample_get_buffer (sample);
    /* Mapping a buffer can fail (non-readable) */
    if (gst_buffer_map (buffer, &map, GST_MAP_READ)) {
#ifdef HAVE_GTK
      pixbuf = gdk_pixbuf_new_from_data (map.data,
          GDK_COLORSPACE_RGB, FALSE, 8, width, height,
          GST_ROUND_UP_4 (width * 3), NULL, NULL);

      /* save the pixbuf */
      gdk_pixbuf_save (pixbuf, "snapshot.png", "png", &error, NULL);
#endif
      gst_buffer_unmap (buffer, &map);
    }
    gst_sample_unref (sample);
  } else {
    g_print ("could not make snapshot\n");
  }

  /* cleanup and exit */
  gst_element_set_state (pipeline, GST_STATE_NULL);
  gst_object_unref (sink);
  gst_object_unref (pipeline);

  exit (0);
}
```

## 强制格式

有时你需要设置特定的格式。你可以使用 capsfilter元素来实现这一点。

例如，如果您需要特定的视频尺寸和颜色格式，或者特定的音频比特率和声道数，可以使用过滤功能强制GstCaps管道采用特定参数。您可以通过在两个元素之间放置一个`<link>` 标签，并在其 `<link>` 标签的 `<compression>` 属性中指定所需的参数来设置链接的 过滤功能。这将仅允许协商与这些功能兼容的类型。capsfilterGstCapscapscapsfilter

另请参阅创建筛选功能。

### 在播放管道中更改格式

在管道中，也可以动态更改格式 。这可以通过更改`<T>` 元素的 `format` 属性 PLAYING来实现。`<T> ` 元素会向上游发送一个事件，使上游元素尝试重新协商新的格式和分配器。这仅在上游元素未在其源填充上使用固定大小时才有效。capscapsfiltercapsfilterRECONFIGURE

以下示例展示了如何在当前状态下更改管道的上限PLAYING：

```c
#include <stdlib.h>

#include <gst/gst.h>

#define MAX_ROUND 100

int
main (int argc, char **argv)
{
  GstElement *pipe, *filter;
  GstCaps *caps;
  gint width, height;
  gint xdir, ydir;
  gint round;
  GstMessage *message;

  gst_init (&argc, &argv);

  pipe = gst_parse_launch_full ("videotestsrc ! capsfilter name=filter ! "
             "ximagesink", NULL, GST_PARSE_FLAG_NONE, NULL);
  g_assert (pipe != NULL);

  filter = gst_bin_get_by_name (GST_BIN (pipe), "filter");
  g_assert (filter);

  width = 320;
  height = 240;
  xdir = ydir = -10;

  for (round = 0; round < MAX_ROUND; round++) {
    gchar *capsstr;
    g_print ("resize to %dx%d (%d/%d)   \r", width, height, round, MAX_ROUND);

    /* we prefer our fixed width and height but allow other dimensions to pass
     * as well */
    capsstr = g_strdup_printf ("video/x-raw, width=(int)%d, height=(int)%d",
        width, height);

    caps = gst_caps_from_string (capsstr);
    g_free (capsstr);
    g_object_set (filter, "caps", caps, NULL);
    gst_caps_unref (caps);

    if (round == 0)
      gst_element_set_state (pipe, GST_STATE_PLAYING);

    width += xdir;
    if (width >= 320)
      xdir = -10;
    else if (width < 200)
      xdir = 10;

    height += ydir;
    if (height >= 240)
      ydir = -10;
    else if (height < 150)
      ydir = 10;

    message =
        gst_bus_poll (GST_ELEMENT_BUS (pipe), GST_MESSAGE_ERROR,
        50 * GST_MSECOND);
    if (message) {
      g_print ("got error           \n");

      gst_message_unref (message);
    }
  }
  g_print ("done                    \n");

  gst_object_unref (filter);
  gst_element_set_state (pipe, GST_STATE_NULL);
  gst_object_unref (pipe);

  return 0;
}
```

注意我们是如何利用gst_bus_poll()较短的超时时间来获取消息，以及引入短暂的睡眠的。

可以为 capsfilter 设置多个功能限制，并用逗号分隔 ;。功能限制器将尝试重新协商，使用列表中第一个可能的格式。

## 动态改变管道

本节我们将讨论一些动态修改管道的技术。我们将具体讨论如何在PLAYING不中断数据流的情况下，在管道处于特定状态时对其进行更改。

构建动态管道时需要考虑一些重要事项：

有insertbin一些switchbin元素可以针对某些动态管道变更的情况，或许能够满足您的需求。

从流水线中移除元素时，请确保未连接的焊盘上没有数据流，否则会导致致命的流水线错误。在取消连接焊盘之前，务必先阻塞源焊盘（推送模式）或接收器焊盘（拉取模式）。另请参阅“更改流水线中的元素”。

向管道添加元素时，请确保在允许数据流之前，将元素置于正确的状态（通常与父元素的状态相同）。新创建的元素处于默认状态， NULL接收到数据时会返回错误。另请参阅“更改管道中的元素”。

在向管道中添加元素时，GStreamer 默认会将该元素的时钟和基准时间设置为管道的当前值。这意味着该元素能够构建与其他管道元素相同的管道运行时间。也就是说，接收器会像管道中的其他接收器一样同步缓冲区，而源生成的缓冲区运行时间也会与其他源匹配。

从上游链中取消链接元素时，务必确保通过EOS向元素接收器垫发送事件并等待其EOS离开元素（使用事件探测）来刷新元素中任何排队的数据。

如果不执行刷新操作，将会丢失未链接元素缓冲的数据。这可能导致简单的帧丢失（例如丢失几帧视频、几毫秒音频等），但如果移除复用器（在某些情况下还会移除编码器或类似元素），则可能导致文件损坏，无法正常播放，因为某些相关的元数据（例如文件头、查找/索引表、内部同步标签）可能无法正确存储或更新。

另请参阅“更改管道中的元素”。

实时源将产生缓冲区，其大小running-time等于管道的当前大小running-time。

没有活动源的流水线会生成从 running-time0 开始的缓冲区。同样，在刷新寻道后，这些流水线会将缓冲区重置running-time为 0。

可以running-time通过修改来更改。为了保持同步，gst_pad_set_offset ()了解管道中各个元素的优先级非常重要。running-time

添加元素可能会改变管道的状态。例如，添加一个非预滚动的接收器会将管道恢复到预滚动状态。移除一个非预滚动的接收器则可能会将管道的状态更改为“已暂停”和“正在播放”。

添加直播源会取消预播放阶段，并将管道置于播放状态。添加任何直播元素也可能会改变管道的延迟。

添加或移除流水线元素可能会改变流水线的时钟选择。如果新添加的元素提供时钟，则流水线最好使用该新时钟。反之，如果移除为流水线提供时钟的元素，则必须选择新的时钟。

添加或移除元素可能会导致上游或下游元素重新协商容量限制和/或分配器。您无需在应用程序中进行任何操作，插件通常会自动适应新的管道拓扑结构，以优化其格式和分配策略。

重要的是，当您在管道中添加、删除或更改元素时，管道可能需要协商新的格式，而这可能会失败。通常，您可以通过在需要的地方插入正确的转换器元素来解决此问题。另请参阅“更改管道中的元素”。

GStreamer 支持几乎所有动态管道修改，但您需要了解一些细节才能避免管道错误。以下章节将演示一些典型的修改用例。

## 管道中元素的变化

```json5
   - ----.      .----------.      .---- -
element1 |      | element2 |      | element3
       src -> sink       src -> sink
   - ----'      '----------'      '---- -
```

我们希望在管道处于播放状态时将 element2 替换为 element4。假设 element2 是一个可视化元素，而您想要在管道中切换该可视化元素。

我们不能直接将 element2 的 sinkpad 与 element1 的 sourcepad 断开连接，因为这会导致 element1 的 sourcepad 处于未连接状态，并在向 sourcepad 推送数据时引发管道中的流式传输错误。正确的做法是在用 element4 替换 element2 之前，先阻塞 element1 sourcepad 的数据流，然后再恢复数据流，具体步骤如下：

使用阻塞焊盘探针阻塞 element1 的源焊盘。当焊盘被阻塞时，将调用探针回调函数。

在阻塞回调函数内部，element1 和 element2 之间没有任何数据流，直到阻塞解除后才会发生数据流。

取消 element1 和 element2 的链接。

确保将数据从 element2 中完全清除。某些元素内部可能保留一些数据，你需要确保不会因为强制将数据从 element2 中清除而丢失任何数据。你可以通过将数据推EOS送到 element2 来实现这一点，如下所示：

在 element2 的源焊盘上放置一个事件探针。

发送EOS到 element2 的接收器引脚。这样可以确保 element2 内部的所有数据都被强制输出。

等待EOS事件出现在 element2 的源焊盘上。EOS收到事件后，将其丢弃并移除事件探针。

取消 element2 和 element3 的链接。现在您还可以从管道中移除 element2 并将其状态设置为NULL。

如果元素4尚未添加到管道中，则将其添加。链接元素4和元素3。链接元素1和元素4。

确保 element4 与管道中其他元素处于相同的状态。它至少应该处于PAUSED能够接收缓冲区和事件的状态。

解除对 element1 源焊盘探针的阻塞。这将允许新数据进入 element4 并继续传输数据。

上述算法在源引脚阻塞时有效，即管道中有数据流时有效。如果没有数据流，暂时更改元素也没有意义，因此该算法PAUSED也可以用于状态更改。

此示例每秒更改一次简单管道上的视频效果：

```c
#include <gst/gst.h>

static gchar *opt_effects = NULL;

#define DEFAULT_EFFECTS "identity,exclusion,navigationtest," \
    "agingtv,videoflip,vertigotv,gaussianblur,shagadelictv,edgetv"

static GstPad *blockpad;
static GstElement *conv_before;
static GstElement *conv_after;
static GstElement *cur_effect;
static GstElement *pipeline;

static GQueue effects = G_QUEUE_INIT;

static GstPadProbeReturn
event_probe_cb (GstPad * pad, GstPadProbeInfo * info, gpointer user_data)
{
  GMainLoop *loop = user_data;
  GstElement *next;

  if (GST_EVENT_TYPE (GST_PAD_PROBE_INFO_DATA (info)) != GST_EVENT_EOS)
    return GST_PAD_PROBE_OK;

  gst_pad_remove_probe (pad, GST_PAD_PROBE_INFO_ID (info));

  /* take next effect from the queue */
  next = g_queue_pop_head (&effects);
  if (next == NULL) {
    GST_DEBUG_OBJECT (pad, "no more effects");
    g_main_loop_quit (loop);
    return GST_PAD_PROBE_DROP;
  }

  g_print ("Switching from '%s' to '%s'..\n", GST_OBJECT_NAME (cur_effect),
      GST_OBJECT_NAME (next));

  gst_element_set_state (cur_effect, GST_STATE_NULL);

  /* remove unlinks automatically */
  GST_DEBUG_OBJECT (pipeline, "removing %" GST_PTR_FORMAT, cur_effect);
  gst_bin_remove (GST_BIN (pipeline), cur_effect);

  /* push current effect back into the queue */
  g_queue_push_tail (&effects, g_steal_pointer (&cur_effect));

  /* add, link and start the new effect */
  GST_DEBUG_OBJECT (pipeline, "adding   %" GST_PTR_FORMAT, next);
  gst_bin_add (GST_BIN (pipeline), next);

  GST_DEBUG_OBJECT (pipeline, "linking..");
  gst_element_link_many (conv_before, next, conv_after, NULL);

  gst_element_set_state (next, GST_STATE_PLAYING);

  cur_effect = next;
  GST_DEBUG_OBJECT (pipeline, "done");

  return GST_PAD_PROBE_DROP;
}

static GstPadProbeReturn
pad_probe_cb (GstPad * pad, GstPadProbeInfo * info, gpointer user_data)
{
  GstPad *srcpad, *sinkpad;

  GST_DEBUG_OBJECT (pad, "pad is blocked now");

  /* remove the probe first */
  gst_pad_remove_probe (pad, GST_PAD_PROBE_INFO_ID (info));

  /* install new probe for EOS */
  srcpad = gst_element_get_static_pad (cur_effect, "src");
  gst_pad_add_probe (srcpad, GST_PAD_PROBE_TYPE_BLOCK |
      GST_PAD_PROBE_TYPE_EVENT_DOWNSTREAM, event_probe_cb, user_data, NULL);
  gst_object_unref (srcpad);

  /* push EOS into the element, the probe will be fired when the
   * EOS leaves the effect and it has thus drained all of its data */
  sinkpad = gst_element_get_static_pad (cur_effect, "sink");
  gst_pad_send_event (sinkpad, gst_event_new_eos ());
  gst_object_unref (sinkpad);

  return GST_PAD_PROBE_OK;
}

static gboolean
timeout_cb (gpointer user_data)
{
  gst_pad_add_probe (blockpad, GST_PAD_PROBE_TYPE_BLOCK_DOWNSTREAM,
      pad_probe_cb, user_data, NULL);

  return TRUE;
}

static gboolean
bus_cb (GstBus * bus, GstMessage * msg, gpointer user_data)
{
  GMainLoop *loop = user_data;

  switch (GST_MESSAGE_TYPE (msg)) {
    case GST_MESSAGE_ERROR:{
      GError *err = NULL;
      gchar *dbg;

      gst_message_parse_error (msg, &err, &dbg);
      gst_object_default_error (msg->src, err, dbg);
      g_clear_error (&err);
      g_free (dbg);
      g_main_loop_quit (loop);
      break;
    }
    default:
      break;
  }
  return TRUE;
}

int
main (int argc, char **argv)
{
  GOptionEntry options[] = {
    {"effects", 'e', 0, G_OPTION_ARG_STRING, &opt_effects,
        "Effects to use (comma-separated list of element names)", NULL},
    {NULL}
  };
  GOptionContext *ctx;
  GError *err = NULL;
  GMainLoop *loop;
  GstElement *src, *q1, *q2, *effect, *filter, *sink;
  gchar **effect_names, **e;

  ctx = g_option_context_new ("");
  g_option_context_add_main_entries (ctx, options, GETTEXT_PACKAGE);
  g_option_context_add_group (ctx, gst_init_get_option_group ());
  if (!g_option_context_parse (ctx, &argc, &argv, &err)) {
    g_error ("Error initializing: %s\n", err->message);
    return 1;
  }
  g_option_context_free (ctx);

  if (opt_effects != NULL)
    effect_names = g_strsplit (opt_effects, ",", -1);
  else
    effect_names = g_strsplit (DEFAULT_EFFECTS, ",", -1);

  for (e = effect_names; e != NULL && *e != NULL; ++e) {
    GstElement *el;

    el = gst_element_factory_make (*e, NULL);
    if (el) {
      g_print ("Adding effect '%s'\n", *e);
      g_queue_push_tail (&effects, gst_object_ref_sink (el));
    }
  }

  pipeline = gst_pipeline_new ("pipeline");

  src = gst_element_factory_make ("videotestsrc", NULL);
  g_object_set (src, "is-live", TRUE, NULL);

  filter = gst_element_factory_make ("capsfilter", NULL);
  gst_util_set_object_arg (G_OBJECT (filter), "caps",
      "video/x-raw, width=320, height=240, "
      "format={ I420, YV12, YUY2, UYVY, AYUV, Y41B, Y42B, "
      "YVYU, Y444, v210, v216, NV12, NV21, UYVP, A420, YUV9, YVU9, IYU1 }");

  q1 = gst_element_factory_make ("queue", NULL);

  blockpad = gst_element_get_static_pad (q1, "src");

  conv_before = gst_element_factory_make ("videoconvert", NULL);

  effect = g_queue_pop_head (&effects);
  cur_effect = effect;

  conv_after = gst_element_factory_make ("videoconvert", NULL);

  q2 = gst_element_factory_make ("queue", NULL);

  sink = gst_element_factory_make ("ximagesink", NULL);

  gst_bin_add_many (GST_BIN (pipeline), src, filter, q1, conv_before, effect,
      conv_after, q2, sink, NULL);

  gst_element_link_many (src, filter, q1, conv_before, effect, conv_after,
      q2, sink, NULL);

  if (gst_element_set_state (pipeline,
          GST_STATE_PLAYING) == GST_STATE_CHANGE_FAILURE) {
    g_error ("Error starting pipeline");
    return 1;
  }

  loop = g_main_loop_new (NULL, FALSE);

  gst_bus_add_watch (GST_ELEMENT_BUS (pipeline), bus_cb, loop);

  g_timeout_add_seconds (1, timeout_cb, loop);

  g_main_loop_run (loop);

  gst_element_set_state (pipeline, GST_STATE_NULL);

  gst_object_unref (blockpad);
  gst_bus_remove_watch (GST_ELEMENT_BUS (pipeline));
  gst_object_unref (pipeline);
  g_main_loop_unref (loop);

  g_queue_clear_full (&effects, (GDestroyNotify) gst_object_unref);
  gst_object_unref (cur_effect);
  g_strfreev (effect_names);

  return 0;
}
```

请注意我们videoconvert在效果前后添加了哪些元素。这是必要的，因为某些元素可能使用不同的色彩空间；通过插入转换元素，我们可以帮助确保能够协商出正确的格式。









