# 播放教程 4：逐行流媒体

## 目标

基础教程 12：流媒体播放展示了如何在网络状况不佳的情况下，通过考虑缓冲问题来提升用户体验。本教程在 基础教程 12：流媒体播放的基础上，进一步扩展了本地存储流媒体内容的功能，并阐述了该技术的优势。具体而言，本教程将展示：

- 如何启用渐进式下载
- 如何知道已下载的内容
- 如何知道它从哪里下载的
- 如何限制下载数据量的保留量

## 介绍

流媒体播放时，数据会从网络获取，并保留少量未来数据缓冲区以确保流畅播放（参见 基础教程 12：流媒体）。但是，数据一旦显示或渲染完毕就会被丢弃（没有历史数据缓冲区）。这意味着，如果用户想要跳转到之前的某个播放点继续播放，则需要重新下载数据。

专为流媒体设计的媒体播放器，例如 YouTube，通常会将所有下载的数据存储在本地以应对这种情况。此外，通常还会使用图形化组件来显示已下载的文件进度。

playbin通过标志提供类似的功能，DOWNLOAD该标志会将媒体存储在本地临时文件中，以便更快地播放已下载的片段。

这段代码还展示了如何使用缓冲查询，从而可以知道文件的哪些部分是可用的。

## 具有本地存储的网络弹性示例

将此代码复制到名为 . 的文本文件中playback-tutorial-4.c。

播放教程 4.c

```c
#include <gst/gst.h>
#include <string.h>

#ifdef __APPLE__
#include <TargetConditionals.h>
#endif

#define GRAPH_LENGTH 78

/* playbin flags */
typedef enum
{
  GST_PLAY_FLAG_DOWNLOAD = (1 << 7)     /* Enable progressive download (on selected formats) */
} GstPlayFlags;

typedef struct _CustomData
{
  gboolean is_live;
  GstElement *pipeline;
  GMainLoop *loop;
  gint buffering_level;
} CustomData;

static void
got_location (GstObject * gstobject, GstObject * prop_object, GParamSpec * prop,
    gpointer data)
{
  gchar *location;
  g_object_get (G_OBJECT (prop_object), "temp-location", &location, NULL);
  g_print ("Temporary file: %s\n", location);
  g_free (location);
  /* Uncomment this line to keep the temporary file after the program exits */
  /* g_object_set (G_OBJECT (prop_object), "temp-remove", FALSE, NULL); */
}

static void
cb_message (GstBus * bus, GstMessage * msg, CustomData * data)
{

  switch (GST_MESSAGE_TYPE (msg)) {
    case GST_MESSAGE_ERROR:{
      GError *err;
      gchar *debug;

      gst_message_parse_error (msg, &err, &debug);
      g_print ("Error: %s\n", err->message);
      g_error_free (err);
      g_free (debug);

      gst_element_set_state (data->pipeline, GST_STATE_READY);
      g_main_loop_quit (data->loop);
      break;
    }
    case GST_MESSAGE_EOS:
      /* end-of-stream */
      gst_element_set_state (data->pipeline, GST_STATE_READY);
      g_main_loop_quit (data->loop);
      break;
    case GST_MESSAGE_BUFFERING:
      /* If the stream is live, we do not care about buffering. */
      if (data->is_live)
        break;

      gst_message_parse_buffering (msg, &data->buffering_level);

      /* Wait until buffering is complete before start/resume playing */
      if (data->buffering_level < 100)
        gst_element_set_state (data->pipeline, GST_STATE_PAUSED);
      else
        gst_element_set_state (data->pipeline, GST_STATE_PLAYING);
      break;
    case GST_MESSAGE_CLOCK_LOST:
      /* Get a new clock */
      gst_element_set_state (data->pipeline, GST_STATE_PAUSED);
      gst_element_set_state (data->pipeline, GST_STATE_PLAYING);
      break;
    default:
      /* Unhandled message */
      break;
  }
}

static gboolean
refresh_ui (CustomData * data)
{
  GstQuery *query;
  gboolean result;

  query = gst_query_new_buffering (GST_FORMAT_PERCENT);
  result = gst_element_query (data->pipeline, query);
  if (result) {
    gint n_ranges, range, i;
    gchar graph[GRAPH_LENGTH + 1];
    gint64 position = 0, duration = 0;

    memset (graph, ' ', GRAPH_LENGTH);
    graph[GRAPH_LENGTH] = '\0';

    n_ranges = gst_query_get_n_buffering_ranges (query);
    for (range = 0; range < n_ranges; range++) {
      gint64 start, stop;
      gst_query_parse_nth_buffering_range (query, range, &start, &stop);
      start = start * GRAPH_LENGTH / (stop - start);
      stop = stop * GRAPH_LENGTH / (stop - start);
      for (i = (gint) start; i < stop; i++)
        graph[i] = '-';
    }
    if (gst_element_query_position (data->pipeline, GST_FORMAT_TIME, &position)
        && GST_CLOCK_TIME_IS_VALID (position)
        && gst_element_query_duration (data->pipeline, GST_FORMAT_TIME,
            &duration) && GST_CLOCK_TIME_IS_VALID (duration)) {
      i = (gint) (GRAPH_LENGTH * (double) position / (double) (duration + 1));
      graph[i] = data->buffering_level < 100 ? 'X' : '>';
    }
    g_print ("[%s]", graph);
    if (data->buffering_level < 100) {
      g_print (" Buffering: %3d%%", data->buffering_level);
    } else {
      g_print ("                ");
    }
    g_print ("\r");
  }

  return TRUE;

}

int
tutorial_main (int argc, char *argv[])
{
  GstElement *pipeline;
  GstBus *bus;
  GstStateChangeReturn ret;
  GMainLoop *main_loop;
  CustomData data;
  guint flags;

  /* Initialize GStreamer */
  gst_init (&argc, &argv);

  /* Initialize our data structure */
  memset (&data, 0, sizeof (data));
  data.buffering_level = 100;

  /* Build the pipeline */
  pipeline =
      gst_parse_launch
      ("playbin uri=https://gstreamer.freedesktop.org/data/media/sintel_trailer-480p.webm",
      NULL);
  bus = gst_element_get_bus (pipeline);

  /* Set the download flag */
  g_object_get (pipeline, "flags", &flags, NULL);
  flags |= GST_PLAY_FLAG_DOWNLOAD;
  g_object_set (pipeline, "flags", flags, NULL);

  /* Uncomment this line to limit the amount of downloaded data */
  /* g_object_set (pipeline, "ring-buffer-max-size", (guint64)4000000, NULL); */

  /* Start playing */
  ret = gst_element_set_state (pipeline, GST_STATE_PLAYING);
  if (ret == GST_STATE_CHANGE_FAILURE) {
    g_printerr ("Unable to set the pipeline to the playing state.\n");
    gst_object_unref (pipeline);
    return -1;
  } else if (ret == GST_STATE_CHANGE_NO_PREROLL) {
    data.is_live = TRUE;
  }

  main_loop = g_main_loop_new (NULL, FALSE);
  data.loop = main_loop;
  data.pipeline = pipeline;

  gst_bus_add_signal_watch (bus);
  g_signal_connect (bus, "message", G_CALLBACK (cb_message), &data);
  g_signal_connect (pipeline, "deep-notify::temp-location",
      G_CALLBACK (got_location), NULL);

  /* Register a function that GLib will call every second */
  g_timeout_add_seconds (1, (GSourceFunc) refresh_ui, &data);

  g_main_loop_run (main_loop);

  /* Free resources */
  g_main_loop_unref (main_loop);
  gst_object_unref (bus);
  gst_element_set_state (pipeline, GST_STATE_NULL);
  gst_object_unref (pipeline);

  g_print ("\n");
  return 0;
}

int
main (int argc, char *argv[])
{
#if defined(__APPLE__) && TARGET_OS_MAC && !TARGET_OS_IPHONE
  return gst_macos_main ((GstMainFunc) tutorial_main, argc, argv, NULL);
#else
  return tutorial_main (argc, argv);
#endif
}
```

    如果您在编译此代码时需要帮助，请参阅 适用于您平台（Mac或 Windows）的“构建教程”部分，或者在 Linux 上使用以下特定命令：
    gcc playback-tutorial-4.c -o playback-tutorial-4 `pkg-config --cflags --libs gstreamer-1.0`
    如果您在运行此代码时需要帮助，请参阅适用于您平台的“运行教程”部分： Mac OS X、Windows、 iOS或android。
    本教程会打开一个窗口并显示一段视频，视频配有相应的音频。媒体文件是从互联网下载的，因此窗口可能需要几秒钟才能显示，具体时间取决于您的网络连接速度。在控制台窗口中，您应该会看到一条消息，指示媒体文件的存储位置，以及一个文本图表，显示已下载的部分和当前播放位置。当需要缓冲时，会显示一条缓冲消息；如果您的网络连接速度足够快，则可能永远不会出现缓冲情况。
    所需库：gstreamer-1.0

## 攻略

这段代码基于基础教程第12章：流式传输。我们只来看看它们之间的区别。

### 设置

```c
/* Set the download flag */
g_object_get (pipeline, "flags", &flags, NULL);
flags |= GST_PLAY_FLAG_DOWNLOAD;
g_object_set (pipeline, "flags", flags, NULL);
```

通过设置此标志，playbin指示其内部队列（ queue2实际上是一个元素）存储所有下载的数据。

```c
g_signal_connect (pipeline, "deep-notify::temp-location", G_CALLBACK (got_location), NULL);
```

deep-notifyGstObject当元素（例如 `<div> playbin`）的任何子元素的属性发生变化时，这些元素会发出信号。在本例中，我们想知道temp-location属性何时发生变化，这表明 `<div>`queue2已决定将下载的数据存储到哪里。

```c
static void got_location (GstObject *gstobject, GstObject *prop_object, GParamSpec *prop, gpointer data) {
  gchar *location;
  g_object_get (G_OBJECT (prop_object), "temp-location", &location, NULL);
  g_print ("Temporary file: %s\n", location);
  g_free (location);
  /* Uncomment this line to keep the temporary file after the program exits */
  /* g_object_set (G_OBJECT (prop_object), "temp-remove", FALSE, NULL); */
}
```

temp-location从触发信号的元素（）读取属性，queue2并将其打印到屏幕上。

当管道状态从变为时PAUSED，READY此文件将被删除。正如注释中所述，您可以通过将 temp-remove属性设置为queue2来保留它FALSE。

    在 Windows 系统中，该文件通常会创建在Temporary Internet Files文件夹内，这可能会导致它在 Windows 资源管理器中隐藏。如果您找不到下载的文件，请尝试使用控制台。

### 用户界面

我们main还安装了一个定时器，用于每秒刷新一次用户界面。

```c
/* Register a function that GLib will call every second */
g_timeout_add_seconds (1, (GSourceFunc)refresh_ui, &data);
```

该refresh_ui方法查询管道，以确定文件哪些部分已下载以及当前播放位置。它构建一个图表来显示这些信息（类似于文本模式的用户界面），并将其打印到屏幕上，覆盖之前的内容，使其看起来像是动画效果：

```c
[---->-------                ]
```

短横线“ -”表示已下载部分，大于号“ >”表示当前位置（X管道暂停时变为“ ”）。请注意，如果您的网络速度足够快，您将看不到下载进度条（短横线）有任何变化；它从一开始就会完全显示。

```c
static gboolean refresh_ui (CustomData *data) {
  GstQuery *query;
  gboolean result;
  query = gst_query_new_buffering (GST_FORMAT_PERCENT);
  result = gst_element_query (data->pipeline, query);
```

我们首先要做的refresh_ui就是创建一个新的 Buffering 对象 GstQuery，gst_query_new_buffering()并将其传递给管道（playbin）gst_element_query()。在基础教程 4：时间管理中，我们已经了解了如何使用特定方法执行诸如 Position 和 Duration 之类的简单查询。更复杂的查询，例如 Buffering，需要使用更通用的方法gst_element_query()。

缓冲查询可以采用不同的格式GstFormat（时间、字节、百分比等等）。并非所有元素都能以所有格式响应查询，因此您需要检查您的特定管道支持哪些格式。如果gst_element_query()返回结果TRUE，则表示查询成功。查询结果包含在 GstQuery我们创建的相同结构中，并且可以使用多种解析方法来检索：

```c
n_ranges = gst_query_get_n_buffering_ranges (query);
for (range = 0; range < n_ranges; range++) {
  gint64 start, stop;
  gst_query_parse_nth_buffering_range (query, range, &start, &stop);
  start = start * GRAPH_LENGTH / (stop - start);
  stop = stop * GRAPH_LENGTH / (stop - start);
  for (i = (gint)start; i < stop; i++)
    graph [i] = '-';
}
```

数据无需从文件开头连续下载：例如，查找操作可能会强制从新的位置开始下载，从而遗漏已下载的数据块。因此，返回已下载数据块（或范围）gst_query_get_n_buffering_ranges()的数量，然后使用检索每个范围的位置和大小。gst_query_parse_nth_buffering_range()

返回值的格式（每个范围的起始位置和结束位置）取决于我们在 gst_query_new_buffering()调用中请求的内容。在本例中，请求的是百分比。这些值用于生成图表。

```c
if (gst_element_query_position (data->pipeline, GST_FORMAT_TIME, &position) &&
    GST_CLOCK_TIME_IS_VALID (position) &&
    gst_element_query_duration (data->pipeline, GST_FORMAT_TIME, &duration) &&
    GST_CLOCK_TIME_IS_VALID (duration)) {
  i = (gint)(GRAPH_LENGTH * (double)position / (double)(duration + 1));
  graph [i] = data->buffering_level < 100 ? 'X' : '>';
}
```

接下来，查询当前位置。虽然可以按百分比格式查询，因此我们使用了与查询范围类似的代码，但目前这种格式对位置查询的支持并不完善。因此，我们使用时间格式，并同时查询持续时间来获取百分比。

>当前位置根据缓冲级别用“ ”或“ ”表示X。如果缓冲级别低于 100%，则 cb_message方法中的代码会将管道设置为PAUSED“ ”，因此我们打印“ X”。如果缓冲级别为 100%，则管道处于“ ”状态 PLAYING，我们打印“ >”。

```c
if (data->buffering_level < 100) {
  g_print (" Buffering: %3d%%", data->buffering_level);
} else {
  g_print ("                ");
}
```

最后，如果缓冲级别低于 100%，我们会报告此信息（否则会将其删除）。

### 限制下载文件的大小

```c
/* Uncomment this line to limit the amount of downloaded data */
/* g_object_set (pipeline, "ring-buffer-max-size", (guint64)4000000, NULL); */
```

取消注释第 139 行，即可查看具体实现方法。此操作会覆盖已播放过的区域，从而减小临时文件的大小。请观察下载进度条，查看文件中保留了哪些区域。

## 结论

本教程已演示：

- 如何使用标志启用渐进式 GST_PLAY_FLAG_DOWNLOAD playbin下载
- 如何使用缓冲功能查看已下载的内容GstQuery
- deep-notify::temp-location如何通过信号知道它被下载到哪里了
- ring-buffer-max-size如何使用属性限制临时文件的大小playbin。

很高兴您能来，期待很快再见到您！










